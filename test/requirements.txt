# test/ 전체가 공유하는 단일 환경 (계획서 7절 Step 0).
#
# detectron2는 여기 없다 - 소스에서 별도로 빌드해야 한다:
#   git clone https://github.com/facebookresearch/detectron2.git
#   pip install -e detectron2/
#
# MSDeformAttn(DINO 쪽 CUDA 확장)도 pip 패키지가 아니라 별도 빌드가 필요하다:
#   cd dit_dino/models/dino/ops && python setup.py build install
#
# dit/requirements.txt의 torch==1.9.0 / timm==0.5.4 고정 버전은 따르지 않는다 -
# MSDeformAttn 빌드 및 최신 CUDA/driver와 충돌한다. beit.py가 쓰는 timm API
# (drop_path, to_2tuple, trunc_normal_)는 timm 0.6.x에서도 동일 경로로 유지된다.

torch>=1.13
torchvision>=0.14
timm==0.6.13

pycocotools
scipy
opencv-python
Pillow
termcolor
addict
yapf
tensorboardX
submitit
