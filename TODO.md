# TODO

- Compare PubLayNet COCO `AP-small` for the reduced 256-channel pyramid against
  the legacy 768-channel pyramid under the same seed, global batch, input
  scales, optimizer-step budget, and evaluation checkpoint.
- 내부 DDP 실행에서 서버별 NUMA binding 성능 영향 측정
