numpy>=1.21
Pillow>=8
torch>=1.9
torchvision>=0.10
pycocotools>=2.0.6
scipy
termcolor
addict
yapf
timm

# Install Detectron2 separately using a build compatible with the selected
# PyTorch and CUDA versions. Build DINO/models/dino/ops before using DINO.

